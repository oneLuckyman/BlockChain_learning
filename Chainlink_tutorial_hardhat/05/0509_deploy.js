async function main() {
    // compile them in our code
    // compile them separately
}

main()
    .then(() => process.exit(0))
    .catch((error) => {
        console.error(error);
        process.exit(1);
    })